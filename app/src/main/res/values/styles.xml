<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Basic Style -->
    <style name="BaseMatchStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="BaseWrapStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="BaseMatchAndWrapStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="BaseWrapAndMatchStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!-- Edit/Insert Data -->

    <style name="InsertAndDeleteData" parent="BaseMatchAndWrapStyle">
        <item name="android:paddingLeft">@dimen/paddingGeneral</item>
        <item name="android:gravity">left</item>
    </style>

    <style name="ItemTag" parent="InsertAndDeleteData">
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Small</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ItemEdit" parent="InsertAndDeleteData">
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Medium</item>
        <item name="android:layout_marginLeft">@dimen/marginDisplayEditData</item>
        <item name="android:layout_marginBottom">@dimen/marginGeneral</item>
    </style>
    
</resources>
